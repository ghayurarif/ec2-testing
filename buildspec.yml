version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 19.x
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - start:prod
artifacts:
  files: dist
